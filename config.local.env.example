# 复制本文件为 config.local.env，填入真实 ARK_API_KEY（config.local.env 已加入 .gitignore）
# 亦可使用系统环境变量 ARK_API_KEY，优先级高于文件中的值。
ARK_API_KEY=

# 以下为可选，与详细设计一致；不配则使用程序内默认值
# CHROMA_PATH=./chroma_db
# LOG_PATH=./logs/session.jsonl
# ARK_MODEL=doubao-seed-2-0-lite-260215
# TOP_K=5
